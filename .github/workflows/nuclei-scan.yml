name: 🔒 Escaneo de Seguridad con Nuclei

on:
  workflow_dispatch: # te permite ejecutarlo manualmente
  push:
    branches:
      - development
      - main

jobs:
  nuclei_scan:
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Descargar código
        uses: actions/checkout@v4

      - name: 🚀 Ejecutar escaneo con Nuclei
        uses: projectdiscovery/nuclei-action@main
        with:
          urls: "https://lab5-t3vd.onrender.com" 
          templates: "cves/, vulnerabilities/, misconfigurations/"
          output: "nuclei-report.txt"

      - name: 📤 Guardar el reporte
        uses: actions/upload-artifact@v4
        with:
          name: nuclei-report
          path: nuclei-report.txt
